[package]
name = "e2e-test"
version = "0.4.0"
edition = "2018"
license-file = "../LICENSE"
publish = false

[dependencies]
radius = { version = "0.4.0", path = "../radius" }
tokio = { version = "1.35.1", features = ["signal", "net"] }
async-trait = "0.1.77"
